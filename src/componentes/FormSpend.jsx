// import React from 'react';
// import { Typography, List, ListItem, ListItemText } from '@mui/material';

// const FormularioList = ({ expenses }) => {
//   return (
//     <div>
//       <Typography variant="h5" sx={{ marginTop: '2rem', marginBottom: '1rem' }}>
//         Lista de Gastos
//       </Typography>
//       <List sx={{ backgroundColor: '#fff' }}>
//         {expenses.map((expense, index) => (
//           <ListItem key={index}>
//             <ListItemText primary={expense.category} secondary={`${expense.concept} - $${expense.amount}`} />
//           </ListItem>
//         ))}
//       </List>
//     </div>
//   );
// };

// export default FormularioList;
